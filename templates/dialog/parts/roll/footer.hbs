<footer class="footer {{ifThen isPJ 'triple' 'double'}}">
    {{#each buttons}}
        <button type="button" class="dialog-button {{@key}}" data-action="{{@key}}">
            {{{icon}}}
            <label>{{label}}</label>
        </button>
    {{/each}}
</footer>